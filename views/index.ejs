<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Council of The Ancients - Dune Awakening Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #B8860B 0%, #CD853F 30%, #8B4513 70%, #654321 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #F5DEB3;
        }
        
        .navbar {
            background: rgba(139, 69, 19, 0.9) !important;
            backdrop-filter: blur(10px);
            border-bottom: 2px solid #D4A574;
        }
        
        .navbar-brand, .navbar-nav .nav-link {
            color: #F5DEB3 !important;
            font-weight: 500;
        }
        
        .hero-section {
            text-align: center;
            color: #F5DEB3;
            padding: 80px 0;
            background: rgba(139, 69, 19, 0.3);
            backdrop-filter: blur(5px);
        }
        
        .card {
            background: rgba(139, 69, 19, 0.8);
            backdrop-filter: blur(10px);
            border: 2px solid #D4A574;
            border-radius: 15px;
            color: #F5DEB3;
        }
        
        .btn-discord {
            background: linear-gradient(45deg, #D4A574, #CD853F);
            border: 2px solid #B8860B;
            color: #8B4513;
            font-size: 1.1rem;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: bold;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
        }
        
        .btn-discord:hover {
            background: linear-gradient(45deg, #CD853F, #B8860B);
            border-color: #8B4513;
            color: #654321;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(212, 165, 116, 0.4);
        }
        
        .feature-icon {
            font-size: 3rem;
            color: #D4A574;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
        
        .alert {
            border-radius: 10px;
            border: none;
        }
        
        .dune-title {
            font-size: 3.5rem;
            font-weight: bold;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.5);
            color: #D4A574;
            margin-bottom: 1rem;
        }
        
        .guild-subtitle {
            font-size: 1.5rem;
            color: #F5DEB3;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 2rem;
        }
        
        .spice-accent {
            color: #D4A574;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="bi bi-gem"></i> Council of The Ancients
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">
                                <i class="bi bi-calendar3"></i> Events Calendar
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/events">
                                <i class="bi bi-list-task"></i> All Events
                            </a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle"></i> <%= user.username %>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="/logout">
                                    <i class="bi bi-box-arrow-right"></i> Logout
                                </a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">
                                <i class="bi bi-box-arrow-in-right"></i> Login
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <main>
        <!-- Flash messages -->
        <div class="container mt-4">
            <% if (typeof messages !== 'undefined' && messages) { %>
                <% if (messages.error && messages.error.length > 0) { %>
                    <% messages.error.forEach(function(error) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <i class="bi bi-exclamation-triangle"></i> <%= error %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    <% }); %>
                <% } %>
                <% if (messages.success && messages.success.length > 0) { %>
                    <% messages.success.forEach(function(success) { %>
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <i class="bi bi-check-circle"></i> <%= success %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    <% }); %>
                <% } %>
            <% } %>
        </div>

        <!-- Hero Section -->
        <section class="hero-section">
            <div class="container">
                <h1 class="dune-title">
                    <i class="bi bi-gem"></i> Council of The Ancients
                </h1>
                <p class="guild-subtitle">
                    Dune Awakening Guild Events & Coordination
                </p>
                <p class="lead mb-5">
                    Join your fellow Fremen in the conquest of Arrakis. 
                    <span class="spice-accent">The spice must flow.</span>
                </p>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <div class="card mx-auto" style="max-width: 500px;">
                        <div class="card-body text-center">
                            <h5 class="card-title">Welcome back, <%= user.username %>!</h5>
                            <p class="card-text">The desert awaits your return. Check the latest guild events and coordinate with your fellow Ancients.</p>
                            <a href="/dashboard" class="btn btn-discord">
                                <i class="bi bi-calendar3"></i> View Events Calendar
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/login" class="btn btn-discord btn-lg">
                        <i class="bi bi-discord"></i> Join the Guild
                    </a>
                <% } %>
            </div>
        </section>

        <!-- Features Section -->
        <section class="py-5">
            <div class="container">
                <div class="row">
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 text-center">
                            <div class="card-body">
                                <i class="bi bi-gem feature-icon"></i>
                                <h5 class="card-title spice-accent">Spice Farming</h5>
                                <p class="card-text">Coordinate spice harvesting operations across the vast dunes of Arrakis. The most valuable resource in the universe awaits.</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 text-center">
                            <div class="card-body">
                                <i class="bi bi-shield feature-icon"></i>
                                <h5 class="card-title spice-accent">Base Raids</h5>
                                <p class="card-text">Strategic assault missions on enemy strongholds. Plan coordinated attacks with your guild members.</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 text-center">
                            <div class="card-body">
                                <i class="bi bi-minecart feature-icon"></i>
                                <h5 class="card-title spice-accent">Ore Farming</h5>
                                <p class="card-text">Mining expeditions for valuable resources. Gather materials to strengthen the guild's position on Arrakis.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="text-center py-4 mt-5" style="background: rgba(139, 69, 19, 0.9); border-top: 2px solid #D4A574;">
        <div class="container">
            <p>&copy; 2025 Council of The Ancients. Created by <strong>NylasDev</strong> - <a href="https://thevikingcompany.eu" class="spice-accent" target="_blank">The Viking Company</a></p>
            <p><small>May your blade chip and shatter. The spice must flow.</small></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
