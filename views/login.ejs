<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COA Events - Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a0f0a 25%, #2d1810 50%, #1a0f0a 75%, #0a0a0a 100%);
            min-height: 100vh;
            font-family: 'Exo 2', sans-serif;
            position: relative;
            overflow-x: hidden;
            color: #e8dcc0;
        }
        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 20% 80%, rgba(255, 140, 0, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(255, 165, 0, 0.08) 0%, transparent 50%),
                radial-gradient(circle at 40% 40%, rgba(139, 69, 19, 0.05) 0%, transparent 50%);
            pointer-events: none;
            z-index: 1;
        }
        
        .desert-particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 2;
        }
        
        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(255, 140, 0, 0.3);
            border-radius: 50%;
            animation: float 8s infinite linear;
        }
        
        @keyframes float {
            0% {
                transform: translateY(100vh) translateX(0);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100px) translateX(100px);
                opacity: 0;
            }
        }
        
        .container {
            position: relative;
            z-index: 3;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 2rem;
        }
        
        .login-card {
            background: linear-gradient(145deg, rgba(26, 15, 10, 0.95), rgba(45, 24, 16, 0.9));
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 140, 0, 0.2);
            border-radius: 24px;
            color: #e8dcc0;
            max-width: 700px;
            min-width: 500px;
            width: 100%;
            box-shadow: 
                0 25px 50px rgba(0, 0, 0, 0.8),
                inset 0 1px 0 rgba(255, 140, 0, 0.1),
                0 0 80px rgba(255, 140, 0, 0.05);
            position: relative;
            overflow: hidden;
            margin: 0 auto;
        }
        
        .login-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, transparent, #ff8c00, transparent);
            animation: shimmer 3s infinite;
        }
        
        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }
        
        .card-body {
            padding: 3rem 2.5rem;
            text-align: center;
            position: relative;
        }
        
        .hero-title {
            font-family: 'Orbitron', monospace;
            font-size: 2.2rem;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #ff8c00, #ffa500, #ff7f00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 30px rgba(255, 140, 0, 0.3);
        }
        
        .subtitle {
            font-size: 1.1rem;
            color: rgba(232, 220, 192, 0.8);
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        
        .btn-discord {
            background: linear-gradient(135deg, #ff8c00 0%, #ff7f00 50%, #e67300 100%);
            border: 2px solid #ff8c00;
            color: #000;
            font-size: 1.2rem;
            font-weight: 700;
            padding: 16px 40px;
            border-radius: 12px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 8px 25px rgba(255, 140, 0, 0.3);
        }
        
        .btn-discord::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }
        
        .btn-discord:hover::before {
            left: 100%;
        }
        
        .btn-discord:hover {
            background: linear-gradient(135deg, #ffa500 0%, #ff8c00 50%, #ff7f00 100%);
            border-color: #ffa500;
            color: #000;
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(255, 140, 0, 0.4);
        }
        
        .discord-icon {
            font-size: 5rem;
            color: #ff8c00;
            margin-bottom: 1.5rem;
            filter: drop-shadow(0 0 20px rgba(255, 140, 0, 0.4));
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .alert {
            border-radius: 12px;
            border: 1px solid rgba(255, 140, 0, 0.2);
            margin-bottom: 1.5rem;
            background: rgba(26, 15, 10, 0.8);
            backdrop-filter: blur(10px);
        }
        
        .alert-danger {
            border-color: rgba(220, 53, 69, 0.3);
            background: rgba(139, 0, 0, 0.1);
        }
        
        .alert-success {
            border-color: rgba(40, 167, 69, 0.3);
            background: rgba(0, 100, 0, 0.1);
        }
        
        .back-link {
            color: rgba(232, 220, 192, 0.6);
            text-decoration: none;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .back-link:hover {
            color: #ff8c00;
            text-decoration: none;
            transform: translateX(-5px);
        }
        
        .spice-accent {
            color: #ff8c00;
            font-weight: 700;
            text-shadow: 0 0 10px rgba(255, 140, 0, 0.3);
        }
        
        .info-text {
            color: rgba(232, 220, 192, 0.7);
            font-size: 0.9rem;
            margin-top: 1.5rem;
        }
        
        .footer-custom {
            background: linear-gradient(135deg, rgba(26, 15, 10, 0.95), rgba(10, 10, 10, 0.98));
            border-top: 1px solid rgba(255, 140, 0, 0.2);
            backdrop-filter: blur(20px);
            color: rgba(232, 220, 192, 0.8);
            padding: 2rem 0;
            margin-top: 3rem;
        }
        
        .access-help {
            color: rgba(232, 220, 192, 0.6);
            font-size: 0.9rem;
            margin-top: 2rem;
        }
    </style>
</head>
<body>
    <!-- Desert particle effect -->
    <div class="desert-particles" id="particles"></div>
    
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <div class="login-card">
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <!-- Discord Logo First -->
                            <i class="bi bi-discord discord-icon mb-3"></i>
                            <br>
                            <!-- COA Logo Under Discord -->
                            <img src="/images/logo/LOGO-32.webp" alt="Council of The Ancients Logo" style="width:80px;height:80px;object-fit:contain;margin-top:1rem;filter:drop-shadow(0 0 16px #ff8c00cc);">
                        </div>
                        
                        <!-- Flash messages -->
                        <% if (typeof messages !== 'undefined' && messages) { %>
                            <% if (messages.error && messages.error.length > 0) { %>
                                <% messages.error.forEach(function(error) { %>
                                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                        <i class="bi bi-exclamation-triangle me-2"></i><%= error %>
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                <% }); %>
                            <% } %>
                            <% if (messages.success && messages.success.length > 0) { %>
                                <% messages.success.forEach(function(success) { %>
                                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                                        <i class="bi bi-check-circle me-2"></i><%= success %>
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                <% }); %>
                            <% } %>
                        <% } %>
                        <h1 class="hero-title">Awaken on Arrakis</h1>
                        <p class="subtitle">
                            Enter the domain of the <span class="spice-accent">Council of The Ancients</span>.
                            <br>Only those blessed by the spice may access the sacred events.
                        </p>
                        
                        <div class="d-grid gap-2 mb-4">
                            <a href="/auth/discord" class="btn btn-discord">
                                <i class="bi bi-discord me-3"></i>
                                Enter via Discord
                            </a>
                        </div>
                        
                        <div class="info-text">
                            <i class="bi bi-shield-check me-2"></i>
                            Secure authentication through Discord
                        </div>
                        
                        <div class="mt-4">
                            <a href="/" class="back-link">
                                <i class="bi bi-arrow-left me-2"></i>
                                Return to the desert
                            </a>
                        </div>
                    </div>
                </div>
                
                <div class="access-help text-center">
                    <p>
                        <i class="bi bi-question-circle me-2"></i>
                        Need access? Contact a Mentat to join our sacred order.
                    </p>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer-custom text-center">
        <div class="container">
            <p class="mb-0">
                &copy; 2025 Council of The Ancients &middot; 
                Forged by <span class="spice-accent">NylasDev</span> &middot; 
                <a href="https://thevikingcompany.eu" class="spice-accent text-decoration-none" target="_blank">The Viking Company</a>
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Create floating particles for desert atmosphere
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 15;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = (8 + Math.random() * 4) + 's';
                particlesContainer.appendChild(particle);
            }
        }
        
        // Initialize particles when page loads
        document.addEventListener('DOMContentLoaded', createParticles);
    </script>
</body>
</html>
