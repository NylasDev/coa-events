<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>COA Events - Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #B8860B 0%, #CD853F 30%, #8B4513 70%, #654321 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #F5DEB3;
        }
        
        .login-card {
            background: rgba(139, 69, 19, 0.8);
            backdrop-filter: blur(10px);
            border: 2px solid #D4A574;
            border-radius: 20px;
            color: #F5DEB3;
            max-width: 400px;
            width: 100%;
        }
        
        .btn-discord {
            background: linear-gradient(45deg, #5865F2, #4752C4);
            border: 2px solid #5865F2;
            color: white;
            font-size: 1.1rem;
            padding: 12px 30px;
            border-radius: 50px;
            transition: all 0.3s ease;
            font-weight: bold;
        }
        
        .btn-discord:hover {
            background: linear-gradient(45deg, #4752C4, #3C4CB8);
            border-color: #4752C4;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(88, 101, 242, 0.4);
        }
        
        .discord-icon {
            font-size: 4rem;
            color: #D4A574;
            margin-bottom: 1rem;
        }
        
        .alert {
            border-radius: 10px;
            border: none;
            margin-bottom: 1.5rem;
        }
        
        .back-link {
            color: rgba(245, 222, 179, 0.7);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        
        .back-link:hover {
            color: #D4A574;
            text-decoration: underline;
        }
        
        .spice-accent {
            color: #D4A574;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="login-card">
                    <div class="card-body p-5 text-center">
                        <!-- Flash messages -->
                        <% if (typeof messages !== 'undefined' && messages) { %>
                            <% if (messages.error && messages.error.length > 0) { %>
                                <% messages.error.forEach(function(error) { %>
                                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                        <i class="bi bi-exclamation-triangle"></i> <%= error %>
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                <% }); %>
                            <% } %>
                            <% if (messages.success && messages.success.length > 0) { %>
                                <% messages.success.forEach(function(success) { %>
                                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                                        <i class="bi bi-check-circle"></i> <%= success %>
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                    </div>
                                <% }); %>
                            <% } %>
                        <% } %>
                        
                        <i class="bi bi-discord discord-icon"></i>
                        <h2 class="mb-4">Login to <span class="spice-accent">COA Events</span></h2>
                        <p class="mb-4">
                            Join the <strong class="spice-accent">Council of The Ancients</strong> on Arrakis.
                            You must be a member of our authorized Discord server to access the spice.
                        </p>
                        
                        <div class="d-grid gap-2">
                            <a href="/auth/discord" class="btn btn-discord">
                                <i class="bi bi-discord me-2"></i>
                                Continue with Discord
                            </a>
                        </div>
                        
                        <div class="mt-4">
                            <small>
                                <i class="bi bi-info-circle me-1"></i>
                                We only access your basic profile information and server membership.
                            </small>
                        </div>
                        
                        <div class="mt-4">
                            <a href="/" class="back-link">
                                <i class="bi bi-arrow-left me-1"></i>
                                Back to Home
                            </a>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-4">
                    <p>
                        <small>
                            Don't have access? Contact an administrator to join the authorized Discord server.
                        </small>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <footer class="text-center py-4 mt-5" style="background: rgba(139, 69, 19, 0.9); border-top: 2px solid #D4A574;">
        <div class="container">
            <p>&copy; 2025 Council of The Ancients. Created by <strong class="spice-accent">NylasDev</strong> - <a href="https://thevikingcompany.eu" class="spice-accent" target="_blank">The Viking Company</a></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
