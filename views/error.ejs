<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Council of The Ancients</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #B8860B 0%, #CD853F 30%, #8B4513 70%, #654321 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #F5DEB3;
        }
        
        .error-container {
            text-align: center;
            max-width: 600px;
            padding: 2rem;
            background: rgba(139, 69, 19, 0.8);
            backdrop-filter: blur(10px);
            border: 2px solid #D4A574;
            border-radius: 20px;
        }
        
        .error-icon {
            font-size: 6rem;
            color: #dc3545;
            margin-bottom: 2rem;
        }
        
        .btn-home {
            background: linear-gradient(45deg, #D4A574, #CD853F);
            border: 2px solid #B8860B;
            color: #8B4513;
            padding: 12px 30px;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: bold;
            transition: all 0.3s ease;
        }
        
        .btn-home:hover {
            background: linear-gradient(45deg, #CD853F, #B8860B);
            color: #654321;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(212, 165, 116, 0.4);
        }
        
        .spice-accent {
            color: #D4A574;
            font-weight: bold;
        }
        
        .error-details {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            padding: 1.5rem;
            margin: 2rem 0;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <i class="bi bi-exclamation-octagon error-icon"></i>
        <h1 class="mb-4">Something Went Wrong on <span class="spice-accent">Arrakis</span></h1>
        <p class="lead mb-4">
            The spice must flow, but <%= message || 'an unexpected error occurred. Please try again later.' %>
        </p>
        
        <% if (typeof error !== 'undefined' && error && error.stack) { %>
            <div class="error-details">
                <h6><i class="bi bi-code-slash me-2"></i>Error Details (Development Mode)</h6>
                <pre style="font-size: 0.8rem; overflow-x: auto; color: #F5DEB3;"><%= error.stack %></pre>
            </div>
        <% } %>
        
        <div class="mb-4">
            <a href="/" class="btn btn-home me-2">
                <i class="bi bi-house me-2"></i>
                Return to Base
            </a>
            <button onclick="window.history.back()" class="btn btn-outline-light">
                <i class="bi bi-arrow-left me-2"></i>
                Go Back
            </button>
        </div>
        
        <p>
            <small>
                <i class="bi bi-info-circle me-1"></i>
                If this error persists, contact the <strong class="spice-accent">Council of The Ancients</strong> administrators.
            </small>
        </p>
    </div>

    <footer class="position-fixed bottom-0 w-100 text-center py-3" style="background: rgba(139, 69, 19, 0.9); border-top: 2px solid #D4A574;">
        <p class="mb-0">&copy; 2025 Council of The Ancients. Created by <strong class="spice-accent">NylasDev</strong> - <a href="https://thevikingcompany.eu" class="spice-accent" target="_blank">The Viking Company</a></p>
    </footer>
</body>
</html>
