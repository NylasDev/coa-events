<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Anomaly - Council of The Ancients</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --dune-black: #0a0a0a;
            --dune-dark: #1a1a1a;
            --dune-orange: #ff6b00;
            --dune-orange-light: #ff8533;
            --dune-orange-dark: #cc5500;
            --dune-gold: #ffb366;
            --dune-text: #ffffff;
            --dune-text-muted: #cccccc;
        }

        * {
            box-sizing: border-box;
        }

        body {
            background: radial-gradient(ellipse at center, var(--dune-dark) 0%, var(--dune-black) 70%);
            min-height: 100vh;
            font-family: 'Exo 2', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--dune-text);
            position: relative;
            overflow: hidden;
        }

        /* Animated background particles */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(2px 2px at 20px 30px, var(--dune-orange), transparent),
                radial-gradient(2px 2px at 40px 70px, var(--dune-gold), transparent),
                radial-gradient(1px 1px at 90px 40px, var(--dune-orange-light), transparent),
                radial-gradient(1px 1px at 130px 80px, var(--dune-orange), transparent),
                radial-gradient(2px 2px at 160px 30px, var(--dune-gold), transparent);
            background-repeat: repeat;
            background-size: 200px 100px;
            animation: float 20s linear infinite;
            opacity: 0.1;
            z-index: -1;
        }

        @keyframes float {
            0% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
            100% { transform: translateY(0px) rotate(360deg); }
        }
        
        .error-container {
            text-align: center;
            max-width: 700px;
            padding: 3rem;
            background: rgba(26, 26, 26, 0.9);
            backdrop-filter: blur(20px);
            border: 2px solid var(--dune-orange);
            border-radius: 20px;
            box-shadow: 
                0 0 50px rgba(255, 107, 0, 0.3),
                inset 0 0 50px rgba(255, 107, 0, 0.1);
            position: relative;
            z-index: 1;
        }

        .error-container::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, var(--dune-orange), var(--dune-gold), var(--dune-orange));
            border-radius: 20px;
            z-index: -1;
            animation: borderGlow 3s ease-in-out infinite alternate;
        }

        @keyframes borderGlow {
            0% { opacity: 0.5; }
            100% { opacity: 1; }
        }
        
        .error-icon {
            font-size: 6rem;
            color: var(--dune-orange);
            margin-bottom: 2rem;
            text-shadow: 0 0 20px var(--dune-orange);
            animation: pulse 2s ease-in-out infinite alternate;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 0.8; }
            100% { transform: scale(1.05); opacity: 1; }
        }

        h1 {
            font-family: 'Orbitron', monospace;
            font-weight: 700;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            text-shadow: 0 0 10px var(--dune-orange);
        }

        .lead {
            font-size: 1.2rem;
            color: var(--dune-text-muted);
            line-height: 1.6;
        }
        
        .btn-home {
            background: linear-gradient(135deg, var(--dune-orange), var(--dune-orange-dark));
            border: 2px solid var(--dune-orange);
            color: var(--dune-text);
            padding: 15px 35px;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            font-family: 'Orbitron', monospace;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 107, 0, 0.3);
        }
        
        .btn-home:hover {
            background: linear-gradient(135deg, var(--dune-orange-light), var(--dune-orange));
            color: var(--dune-text);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 107, 0, 0.5);
        }

        .btn-outline-light {
            border: 2px solid var(--dune-text-muted);
            color: var(--dune-text-muted);
            padding: 15px 35px;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            font-family: 'Orbitron', monospace;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            background: transparent;
        }

        .btn-outline-light:hover {
            background: var(--dune-text-muted);
            color: var(--dune-black);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
        }
        
        .spice-accent {
            color: var(--dune-orange);
            font-weight: bold;
            text-shadow: 0 0 5px var(--dune-orange);
        }
        
        .error-details {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 107, 0, 0.3);
            border-radius: 15px;
            padding: 2rem;
            margin: 2rem 0;
            text-align: left;
        }

        .error-details h6 {
            color: var(--dune-orange);
            font-family: 'Orbitron', monospace;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .error-details pre {
            background: rgba(0, 0, 0, 0.5);
            border-radius: 10px;
            padding: 1rem;
            border: 1px solid rgba(255, 107, 0, 0.2);
        }
    </style>
</head>
<body>
    <div class="error-container">
        <i class="bi bi-exclamation-octagon error-icon"></i>
        <h1 class="mb-4">System Anomaly Detected</h1>
        <p class="lead mb-4">
            The spice mining operation has encountered an unexpected <span class="spice-accent">anomaly</span>. 
            <%= message || 'Imperial systems are responding. Please stand by for restoration.' %>
        </p>
        
        <% if (typeof error !== 'undefined' && error && error.stack) { %>
            <div class="error-details">
                <h6><i class="bi bi-code-slash me-2"></i>Mentat Analysis (Development Mode)</h6>
                <pre style="font-size: 0.8rem; overflow-x: auto; color: var(--dune-text-muted);"><%= error.stack %></pre>
            </div>
        <% } %>
        
        <div class="mb-4">
            <a href="/" class="btn btn-home me-3">
                <i class="bi bi-house me-2"></i>
                Return to Command Center
            </a>
            <button onclick="window.history.back()" class="btn btn-outline-light">
                <i class="bi bi-arrow-left me-2"></i>
                Previous Station
            </button>
        </div>
        
        <p>
            <small style="color: var(--dune-text-muted);">
                <i class="bi bi-info-circle me-1"></i>
                If this anomaly persists, contact the <strong class="spice-accent">Mentat Council</strong> administrators.
            </small>
        </p>
    </div>

    <footer class="position-fixed bottom-0 w-100 text-center py-3" style="background: rgba(26, 26, 26, 0.9); border-top: 2px solid var(--dune-orange); backdrop-filter: blur(10px);">
        <p class="mb-0" style="color: var(--dune-text-muted);">&copy; 2025 Council of The Ancients. Created by <strong class="spice-accent">NylasDev</strong> - <a href="https://thevikingcompany.eu" class="spice-accent" target="_blank" style="text-decoration: none;">The Viking Company</a></p>
    </footer>
</body>
</html>
