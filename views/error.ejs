<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - COA Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }
        
        .error-container {
            text-align: center;
            max-width: 600px;
            padding: 2rem;
        }
        
        .error-icon {
            font-size: 6rem;
            color: #dc3545;
            margin-bottom: 2rem;
        }
        
        .btn-home {
            background: #5865F2;
            border-color: #5865F2;
            color: white;
            padding: 12px 30px;
            border-radius: 50px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        
        .btn-home:hover {
            background: #4752C4;
            border-color: #4752C4;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(88, 101, 242, 0.4);
        }
        
        .error-details {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            padding: 1.5rem;
            margin: 2rem 0;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <i class="bi bi-exclamation-octagon error-icon"></i>
        <h1 class="mb-4">Something Went Wrong</h1>
        <p class="lead mb-4">
            <%= message || 'An unexpected error occurred. Please try again later.' %>
        </p>
        
        <% if (typeof error !== 'undefined' && error && error.stack) { %>
            <div class="error-details">
                <h6><i class="bi bi-code-slash me-2"></i>Error Details (Development Mode)</h6>
                <pre class="text-light" style="font-size: 0.8rem; overflow-x: auto;"><%= error.stack %></pre>
            </div>
        <% } %>
        
        <div class="mb-4">
            <a href="/" class="btn btn-home me-2">
                <i class="bi bi-house me-2"></i>
                Go Back Home
            </a>
            <button onclick="window.history.back()" class="btn btn-outline-light">
                <i class="bi bi-arrow-left me-2"></i>
                Go Back
            </button>
        </div>
        
        <p class="text-light">
            <small>
                If this problem persists, please contact the administrator with the error details above.
            </small>
        </p>
    </div>
</body>
</html>
